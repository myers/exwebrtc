defmodule IntegrationTest do
  use ExUnit.Case
  use Hound.Helpers

  hound_session

  # test "make webrtc connection", meta do
  #   navigate_to("http://localhost:8080/")

  #   find_element(:id, "startButton")
  #   |> click()

  #   assert page_title() == "Thank you"
  # end

end
